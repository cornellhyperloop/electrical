/*/* Primary function of the long range sensor: to monitor the position of the pod relative to the track./* Length of track: 4150 feet (1.25 km)/* Input should be in feet./* threshold variable: distance at which the pod should start braking./* Once the data reading drops below that value, it should signal that the pod should start braking.*/const float threshold = 100; //distance from the end of the trackconst float length = 4150;int checkValidInput(float input) {	float valid = length - input;	if (valid < 0 || valid > length) {		return 0;	}	return 1;}int shouldBrake(float data_reading) {	if (data_reading > threshold && checkValidInput(data_reading) == 1) {		return 1; //don't brake yet	} 	else {		return 0; //start braking}int main(){    printf("%d", shouldBrake(500));    return 0;}